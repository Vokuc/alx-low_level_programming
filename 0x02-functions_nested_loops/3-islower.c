#include "main.h"

/**
 * _islower - functions entry point
 *
 * Return: return 0 for int function
 */
int _islower(int c)
{
	int a = 1;
	int b = 0;

	if (c == islower(c))
	{
		_putchar(a);
	}
	else
	{
		_puchar(b);
	}
	_putchar('\n');
	return (0);
}
