#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: return 0 always
 */
int main(void)
{
	int a;

	int b = a/2;

	for (let a = 'a'; a <= 'z'; a++)
	{
		putchar(b);
	}
	putchar('\n');

	return (0);
}
